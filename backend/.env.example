# ──────────────────────────────────────────────────────────────────────────────
# .env.example — Viwo Bot Backend environment variables
# Copy to .env and fill in your actual keys. Never commit .env to Git!
# ──────────────────────────────────────────────────────────────────────────────

# ── OpenClaw (AI Brain) ────────────────────────────────────────────────────────
# OpenClaw is model-agnostic: it routes to Claude, OpenAI, Gemini, etc.
# The model backend is chosen during `openclaw onboard` — set it there, not here.
#
# Session ID used to maintain Viwo Bot conversation history inside OpenClaw.
# Change this if you want a fresh conversation context.
OPENCLAW_SESSION_ID=viwobot-main
#
# Max seconds to wait for an OpenClaw agent reply (default: 60)
OPENCLAW_TIMEOUT=60
#
# Thinking level passed to openclaw agent (off|minimal|low|medium|high|xhigh)
# Only affects models that support extended thinking (e.g. Claude Opus 4.6)
OPENCLAW_THINKING=off

# ── ElevenLabs TTS (optional — omit to use pyttsx3 offline fallback) ──────────
# Get your key at: https://elevenlabs.io/ → Profile → API Keys
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
# Optional: override the default voice (Rachel = 21m00Tcm4TlvDq8ikWAM)
# Browse voices at https://elevenlabs.io/voice-library
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# ── Picovoice Porcupine (wake word detection) ─────────────────────────────────
# Get your free key at: https://console.picovoice.ai/
PORCUPINE_ACCESS_KEY=your_porcupine_access_key_here
# Optional: path to custom "Hey Viwo" .ppn file
# Train for free at console.picovoice.ai → Custom Wake Word → type "Hey Viwo"
PORCUPINE_KEYWORD_PATH=

# ── STT Engine ────────────────────────────────────────────────────────────────
# "google" (default, online) or "whisper" (offline, requires openai-whisper)
STT_ENGINE=google
